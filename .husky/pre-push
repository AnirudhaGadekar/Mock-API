#!/usr/bin/env sh

# Lightweight pre-push hook.
# We intentionally avoid running heavy scans / production builds here
# because they were failing on Windows due to Prisma DLL locks.
# Instead, we just run the fast critical tests.

npm run test:critical
npm run scan:all